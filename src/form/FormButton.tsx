import {createFormControl} from "axii-ui";
import {Button} from "../../lib/Button.js";

export const FormSubmit = createFormControl(Button, 'submit')
export const FormReset = createFormControl(Button, 'reset')
export const FormClear = createFormControl(Button, 'clear')
